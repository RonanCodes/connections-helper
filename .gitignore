# Dependencies
node_modules/
dist/
dist-ssr/
.turbo/
.pnpm-store/

# Env (only .env.example is committed; every operator brings their own real keys)
.env
.env.local
.env.*.local
.env.production
.env.production.*
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.sw?

# Test output
test-results/
playwright-report/
.playwright-mcp/
.lighthouseci/

# Wrangler / Cloudflare
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# TanStack (generated)
src/routeTree.gen.ts
.tanstack/
.claude/

# Generated OpenAPI dump (regenerated by `pnpm mock`)
openapi.json

# Load test output (regenerated by `pnpm loadtest:*`)
loadtest-summary.json

# Local-factory artefacts (Ralph / planner-worker / night-shift, etc).
# See ~/Dev/ronan-skills/skills/ralph/SKILL.md § "Run artefacts (the canonical shape)".
# patterns.md and *.session.md stay tracked; everything else under .ralph/ is ephemeral.
.ralph/sessions/
.ralph/*.progress.txt
.ralph/.gitignore-policy
