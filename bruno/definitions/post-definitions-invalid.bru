meta {
  name: POST /api/definitions (invalid body)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/definitions
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "wrongKey": true
  }
}

assert {
  res.status: eq 400
  res.body.error: isString
}
